import { type PathsStructure } from "./types";

const paths: PathsStructure = {
  root: "/",
  user: "/user",
  login: "/login",
  register: "/register",
  beaches: "/beaches",
  delete: "/delete/:id",
  filter: "/filter",
  getBeach: "/:id",
};

export default paths;
